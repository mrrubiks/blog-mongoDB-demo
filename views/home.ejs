<%- include("partials/header") %> 
<h1>Home</h1>
<p> <%= homeContent %> </p>
<% posts.forEach(function(post){ %>
    <h1><a><%= post.title %></a></h1>
    <p><%= post.content.substring(0, 100) + (post.content.length > 100 ? "..." : "") %> <a href="/posts/<%= post.title %>"> Read More</a></p>
    
<% }) %>

<%- include("partials/footer") %>